# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/
.mypy_cache/

# Node/React
node_modules/
dist/
build/
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

# Project Specific
backend/.env
AI_Energy_Optimization/backend/.env
.env
data/wastage_anomalies.csv
models/*.pkl
!models/energy_forecast_model.pkl  # Keep the model if it's not too huge, but usually better to ignore large files. 
# For now, let's ignore large models if they exceed 100MB, but Random Forest is usually small enough. 
# I'll allow the model for easier cloning, unless user objects.

# IDE
.vscode/
.idea/
*.swp
